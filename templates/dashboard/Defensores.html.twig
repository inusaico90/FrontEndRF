{% extends 'base.html.twig' %}
{% block head %}
    <title>Homicidios a quien defiende</title>
    {{ parent() }}
{% endblock %}
{% block cabeza %}
<body id="inner_page">
{{ parent() }}
{% endblock %}

{% block cuerpo %}
    {{ parent() }}
    <!-- section -->
    <section class="main_full inner_page">
        <div class="container-fluid">
            <div class="row">
                <div class="col-lg-12 text_align_center">
                    <div class="full heading_s1 center">
                        <h2>Registro Nacional Consolidado. Homicidio de defensores de DDHH 2016 - 2021</h2>
                    </div>
                </div>
            </div>
        </div>
    </section>
    <!-- end section -->
    <!-- section -->
    <section class="layout_padding section" style="width: 82%">
        <div class="container">
            <div class="row" style="justify-content: center">
                <pre>
                <table class="table table-striped">
                    <thead>
                    <tr style="width: 200px;">
                        <th style="text-align: center;">N°</th>
                        <th style="text-align: center;">FECHA</th>
                        <th style="text-align: center;">NOMBRE</th>
                        <th style="text-align: center;">MUNICIPIO</th>
                        <th style="text-align: center;">DEPARTAMENTO</th>
                    </tr>
                    </thead>
                    {% for Todo in Todo %}
                        <tr>
                            <td style="text-align: center;">{{ Todo.N }}</td>
                            <td style="text-align: center;">{{ Todo.fecha | date("d/m/Y") }}</td>
                            <td style="text-align: center;">{{ Todo.Nombre }}</td>
                            <td style="text-align: center;">{{ Todo.Municipio }}</td>
                            <td style="text-align: center;">{{ Todo.Departamento }}</td>
                        </tr>
                    {% endfor %}
                </table>
                </pre>
            </div>
        </div>
        <!--Paginación
        <div class="row mx-0">
            <div class="col-md-12 full center">
                { if previous >=0 %}
                    <button><a href="{ path('DCAccionesUrgentes',{ offset:previous}) }}" class="btn_pagging"><i
                                    class="fa fa-long-arrow-left"></i>Anterior</a></button>
                { endif %}
                { if next < Todo|length %}
                    <button><a href="{ path('DCAccionesUrgentes',{ offset:next}) }}"
                               class="btn_pagging">Siguiente<i
                                    class="fa fa-long-arrow-right"></i></a></button>
                { endif %}
            </div>
        </div>-->
        </div>
    </section>
    <!-- end section -->
{% endblock %}

{% block cierre %}
    {{ parent() }}
{% endblock %}
{% block Copyright %}
    {{ parent() }}
{% endblock %}
